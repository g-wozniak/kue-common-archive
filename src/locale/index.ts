import {Locale} from '@intf/Locale'
import enGB from '@root/locale/en_GB'

// translation: common_Translations

/**
 * locale
 * @description function returns en-GB common translation
 */
const locale = (): Locale => {
   return enGB
}

export default locale

